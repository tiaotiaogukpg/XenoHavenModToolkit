<Window x:Class="XenoHavenModToolkit.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置" Height="360" Width="720"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Theme.WindowBackground}">
    <Grid Margin="16" Background="{DynamicResource Theme.WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Mod 总览目录（包含多个 Mod 文件夹）" Margin="0,0,0,6"/>
        <DockPanel Grid.Row="1" Margin="0,0,0,14">
            <TextBox x:Name="OverviewRootBox" Margin="0,0,8,0"/>
            <Button Content="浏览..." Width="100" Click="Browse_Click"/>
        </DockPanel>

        <TextBlock Grid.Row="2" Text="主题" Margin="0,0,0,6"/>
        <ComboBox x:Name="ThemeBox" Grid.Row="3" SelectedValuePath="Tag" Margin="0,0,0,14">
            <ComboBoxItem Content="明亮模式" Tag="Light"/>
            <ComboBoxItem Content="暗色模式" Tag="Dark"/>
        </ComboBox>

        <CheckBox Grid.Row="4" x:Name="OpenLastModCheck" Content="启动时自动打开上次的 Mod" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="取消" Width="100" IsCancel="True"/>
            <Button Content="保存" Width="120" IsDefault="True" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>

